for (i in 1:10) plot(runif(i))

while (i>0) { i<- i-1; print ("while") }


> mystats <-function(x){
+ x<-x*100
+ }

-transponiting
t(zz)

#reshape lib to split data and aggregate it
melt() -> cast()


a[1:10,1:3] - select 10 rows and 3 columns
a[-1] - remove 1 column


/write file
write.table(a,"c:/a.txt", sep="\t")

a<- read.table("c:/a.txt", sep="\t")

merge - combine columns

rbind - combine rows

// make 2 columns massive from data.frame
x=stack(b)

'rename column name
colnames(z)[1]<-'x'
colnames(z)[2]<-'y'